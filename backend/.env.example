# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/intellifolio
DEBUG=false

# Firebase Configuration (if using)
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_PROJECT_ID=your_firebase_project_id

# GitHub API
GITHUB_API_TOKEN=your_github_token

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key
# Optional: use a different key only for AI suggestions (portfolio strength + suggestions). If not set, GEMINI_API_KEY is used.
# GEMINI_API_KEY_SUGGESTIONS=your_other_gemini_api_key

# Optional: use OpenRouter for AI suggestions instead of Gemini. If set, suggestions use OpenRouter (get key at https://openrouter.ai/keys).
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free

# Server
PORT=8000
HOST=0.0.0.0
